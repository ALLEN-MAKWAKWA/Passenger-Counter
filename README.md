# Passenger-Counter
A The Passenger Counter App is a lightweight web application built using HTML, CSS, and JavaScript. It allows users to increment and save passenger counts dynamically. The project demonstrates core JavaScript concepts such as DOM manipulation along with responsive UI styling using CSS.
